services:
  - type: web
    name: bulk-stk-push
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: API_URL
        value: https://pay.cloud.or.ke/api/wallet/deposit
      - key: BEARER_TOKEN
        sync: false
